services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /data/journal.db
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./data:/data
    ports:
      - "8080:8080"   # TradingView webhook receiver (enable WEBHOOK_ENABLED=true in .env)
